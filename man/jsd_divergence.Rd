% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_functions.R
\name{jsd_divergence}
\alias{jsd_divergence}
\title{Jensen-Shannon divergence between two stem size distributions}
\usage{
jsd_divergence(true_dist, pred_dist, eps = 1e-09)
}
\arguments{
\item{true_dist}{Numeric vector. Reference stem size distribution.}

\item{pred_dist}{Numeric vector. Predicted stem size distribution, same
length and DBH-class order as `true_dist`.}

\item{eps}{Numeric. Additive smoothing constant applied before
renormalising. Default `1e-9`.}
}
\value{
Numeric scalar: Jensen-Shannon divergence in nats,
  bounded `[0, ln(2)]`.
}
\description{
Symmetric, bounded companion metric to [kl_divergence_metric()]
  for pairwise SSD comparisons. JSD is bounded in `[0, ln(2)]`
  (\eqn{\approx 0.693} nats) regardless of distribution distance, making it
  more robust than unbounded KL when comparing predicted and observed stem
  size distributions across stands with extreme structural differences.
  Both inputs are renormalised internally, so absolute stem counts or
  relative proportions are both accepted.
}
\examples{
jsd_divergence(c(400, 200, 100, 50, 20, 10, 5, 2, 1, 0),
               c(380, 210, 110, 45, 22, 12,  4, 3, 1, 0))
}
\seealso{
[kl_divergence_metric()] for the row-wise KL metric used in model
  training; [compute_prediction_metrics()] for the full per-stand metric
  suite.
}
